name: ImmortalWrt Firmware (x86_64)

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Free up disk space
      uses: jlumbroso/free-disk-space@main
      with:
        tool-cache: false
        android: true
        dotnet: true
        haskell: true
        large-packages: true
        docker-images: true
        swap-storage: true

    - name: Install build dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y ack antlr3 asciidoc autoconf automake binutils bison build-essential bzip2 ccache clang cmake cpio curl device-tree-compiler ecj fastjar flex gawk gettext gcc-multilib g++-multilib git gnutls-dev gperf haveged help2man intltool lib32gcc-s1 libc6-dev-i386 libelf-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool lrzsz mkisofs msmtp nano ninja-build p7zip p7zip-full patch pkgconf python2.7 python3 python3-pip python3-ply python3-docutils python3-pyelftools python3-setuptools qemu-utils re2c rsync scons squashfs-tools subversion swig texinfo uglifyjs unzip vim wget xmlto xxd zlib1g-dev
        # Install Go for OpenClash
        wget -q https://go.dev/dl/go1.22.4.linux-amd64.tar.gz
        sudo tar -C /usr/local -xzf go1.22.4.linux-amd64.tar.gz
        echo 'export PATH=$PATH:/usr/local/go/bin' >> $HOME/.bashrc
        echo 'export GOPATH=$HOME/go' >> $HOME/.bashrc
        echo 'export PATH=$PATH:$GOPATH/bin' >> $HOME/.bashrc
        export PATH=$PATH:/usr/local/go/bin
        export GOPATH=$HOME/go
        go version

    - name: Initialize build environment
      run: |
        sudo timedatectl set-timezone "Asia/Shanghai"
        sudo mkdir -p /workdir
        sudo chown ${USER}:${GROUPS} /workdir
        df -hT

    - name: Clone source code
      working-directory: /workdir
      run: |
        git clone -b openwrt-24.10 --depth 1 https://github.com/immortalwrt/immortalwrt.git
        cd immortalwrt
        echo "REPO_NAME=immortalwrt" >> $GITHUB_ENV

    - name: Update and install feeds
      working-directory: /workdir/${{ env.REPO_NAME }}
      run: |
        # iStore feed
        echo 'src-git istore https://github.com/linkease/istore.git;main' >> feeds.conf.default
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: Load custom configuration (x86_64)
      working-directory: /workdir/${{ env.REPO_NAME }}
      run: |
        cat > .config << 'EOF'
        # Target System - x86_64
        CONFIG_TARGET_x86=y
        CONFIG_TARGET_x86_64=y
        CONFIG_TARGET_x86_64_DEVICE_generic=y

        # Target Images - include squashfs and ext4, generate combined image
        CONFIG_TARGET_ROOTFS_EXT4FS=y
        CONFIG_TARGET_ROOTFS_SQUASHFS=y
        CONFIG_TARGET_IMAGES_GZIP=y
        CONFIG_TARGET_ROOTFS_PARTSIZE=4096

        # General packages
        CONFIG_PACKAGE_ipv6=y
        CONFIG_PACKAGE_curl=y
        CONFIG_PACKAGE_wget-ssl=y
        CONFIG_PACKAGE_htop=y
        CONFIG_PACKAGE_nano=y
        CONFIG_PACKAGE_openssh-sftp-server=y

        # LuCI
        CONFIG_PACKAGE_luci=y
        CONFIG_PACKAGE_luci-base=y
        CONFIG_PACKAGE_luci-compat=y
        CONFIG_PACKAGE_luci-lib-ipkg=y
        CONFIG_PACKAGE_luci-app-firewall=y
        CONFIG_PACKAGE_luci-app-opkg=y
        CONFIG_PACKAGE_luci-theme-bootstrap=y
        CONFIG_PACKAGE_luci-theme-argon=y

        # USB / Storage
        CONFIG_PACKAGE_kmod-usb-core=y
        CONFIG_PACKAGE_kmod-usb2=y
        CONFIG_PACKAGE_kmod-usb3=y
        CONFIG_PACKAGE_kmod-usb-storage=y
        CONFIG_PACKAGE_kmod-fs-ext4=y
        CONFIG_PACKAGE_kmod-fs-vfat=y
        CONFIG_PACKAGE_kmod-nls-cp437=y
        CONFIG_PACKAGE_kmod-nls-iso8859-1=y

        # Network
        CONFIG_PACKAGE_dnsmasq-full=y
        CONFIG_PACKAGE_dnsmasq_full_dhcp=y
        CONFIG_PACKAGE_dnsmasq_full_dhcpv6=y
        CONFIG_PACKAGE_dnsmasq_full_ipset=y
        CONFIG_PACKAGE_dnsmasq_full_nftset=y
        CONFIG_PACKAGE_odhcp6c=y
        CONFIG_PACKAGE_odhcpd-ipv6only=y
        CONFIG_PACKAGE_openssh-server-default=y
        CONFIG_PACKAGE_iptables-nft=y

        # Docker
        CONFIG_PACKAGE_luci-app-dockerman=y
        CONFIG_PACKAGE_docker-ce=y
        CONFIG_PACKAGE_docker-ce-cli=y
        CONFIG_PACKAGE_containerd=y
        CONFIG_PACKAGE_docker-compose=y

        # 4G/5G & tethering
        CONFIG_PACKAGE_kmod-usb-net=y
        CONFIG_PACKAGE_kmod-usb-net-cdc-ether=y
        CONFIG_PACKAGE_kmod-usb-net-cdc-mbim=y
        CONFIG_PACKAGE_kmod-usb-net-cdc-ncm=y
        CONFIG_PACKAGE_kmod-usb-net-huawei-cdc-ncm=y
        CONFIG_PACKAGE_kmod-usb-net-qmi-wwan=y
        CONFIG_PACKAGE_kmod-usb-net-rndis=y
        CONFIG_PACKAGE_kmod-usb-serial-option=y
        CONFIG_PACKAGE_kmod-usb-serial-qualcomm=y
        CONFIG_PACKAGE_uqmi=y
        CONFIG_PACKAGE_usb-modeswitch=y
        CONFIG_PACKAGE_comgt=y
        CONFIG_PACKAGE_wwan=y

        # MWAN3
        CONFIG_PACKAGE_mwan3=y
        CONFIG_PACKAGE_luci-app-mwan3=y

        # TurboACC & ttyd
        CONFIG_PACKAGE_luci-app-turboacc=y
        CONFIG_PACKAGE_luci-app-ttyd=y

        # iStore & OpenClash
        CONFIG_PACKAGE_luci-app-store=y
        CONFIG_PACKAGE_luci-app-openclash=y
        EOF
        make defconfig

    - name: Generate firmware
      working-directory: /workdir/${{ env.REPO_NAME }}
      run: |
        make download -j$(nproc)
        make -j$(nproc) || make -j1 V=s

    - name: Organize firmware files
      working-directory: /workdir/${{ env.REPO_NAME }}
      run: |
        rm -rf /tmp/firmware
        mkdir -p /tmp/firmware/packages /tmp/firmware/images

        # Copy all IPK packages
        find bin/packages -name "*.ipk" -exec cp {} /tmp/firmware/packages/ \; 2>/dev/null || true

        # Copy firmware images (combined, rootfs, etc.)
        find bin/targets/x86/64 -type f \( -name "*.img.gz" -o -name "*.tar.gz" -o -name "*.manifest" \) -exec cp {} /tmp/firmware/images/ \; 2>/dev/null || true

        # Copy build.config (the .config file)
        cp .config /tmp/firmware/build.config

        echo "=== Firmware artifacts ==="
        ls -laR /tmp/firmware/

    - name: Upload firmware to GitHub Actions
      uses: actions/upload-artifact@v4
      with:
        name: ImmortalWrt-x86_64-${{ github.run_id }}
        path: /tmp/firmware/

    - name: Generate release tag
      id: tag
      run: |
        echo "RELEASE_TAG=build-$(date +'%Y.%m.%d.%H%M')" >> $GITHUB_OUTPUT

    - name: Upload firmware to release
      uses: softprops/action-gh-release@v2
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ steps.tag.outputs.RELEASE_TAG }}
        name: ImmortalWrt x86_64 Build ${{ github.run_id }}
        files: |
          /tmp/firmware/packages/*.ipk
          /tmp/firmware/images/*
          /tmp/firmware/build.config
        body: |
          ## ImmortalWrt x86_64 Firmware & IPK Build
          - Build ID: ${{ github.run_id }}

          ### Features
          - 🔹 x86_64 通用平台
          - 🔹 4G/5G 模组支持 (QMI/NCM/RNDIS)
          - 🔹 安卓 USB 网络共享支持
          - 🔹 Docker & Docker Compose 支持
          - 🔹 OpenClash 支持
          - 🔹 iStore 应用商店

          ### 默认登录
          - 地址: 192.168.1.1
          - 密码: password

          ### 文件说明
          - `packages/*.ipk` – 可单独安装的软件包
          - `images/*` – 完整固件镜像（如 `combined.img.gz` 可直写磁盘）
          - `build.config` – 本次编译的完整 `.config` 配置文件
        draft: false
        prerelease: false
        fail_on_unmatched_files: false
